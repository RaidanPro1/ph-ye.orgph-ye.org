# ======================================================================
# 🇾🇪 YemenJPT Platform Environment Configuration (V18.1 - Complete Stack Edition)
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to '.env' (`cp .env.example .env`).
# 2. Fill in ALL required values. Use a password manager for strong passwords.

# --- Domain (Required) ---
DOMAIN=localhost

# --- Unified Passwords (Required) ---
# Generate strong, unique passwords for these. UNIFIED_PASS is used for multiple services.
UNIFIED_PASS=generate_a_strong_password
MARIADB_ROOT_PASSWORD=generate_another_strong_password
POSTGRES_PASSWORD=generate_a_postgres_password

# --- PostgreSQL Configuration (Required) ---
POSTGRES_USER=yemenjpt_user
POSTGRES_DB=yemenjpt_db

# --- Keycloak Identity Provider (Required) ---
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=${UNIFIED_PASS} # Uses the unified password by default

# --- TYPO3 CMS (Required) ---
TYPO3_DB_NAME=typo3
TYPO3_DB_USER=typo3_user
TYPO3_DB_PASS=generate_a_strong_typo3_password
TYPO3_ADMIN_USER=admin

# --- CiviCRM Database (Required) ---
CIVICRM_DB_NAME=civicrm
CIVICRM_DB_USER=civicrm_user
CIVICRM_DB_PASS=generate_a_strong_civicrm_password

# --- SMTP Relay Configuration (Highly Recommended) ---
# Used by services like Ghost, Nextcloud, etc., to send emails reliably.
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_or_ses_api_key
SMTP_FROM_ADDRESS=noreply@ph-ye.org
SMTP_FROM_NAME="Press House Platform"

# --- AI Services (Optional) ---
# Your Google Gemini API Key. Required for the cloud AI provider.
API_KEY=your_google_gemini_api_key_here

# --- Telegram Bot for Notifications (Optional) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_ROOT_CHAT_ID=

# --- Dashy Admin API Key (Optional but recommended) ---
# Generate a random string for the panic button API key in dashy-admin.yml
ADMIN_API_KEY=generate_a_random_api_key_for_dashy